# Project version
version=0.0.2-SNAPSHOT

# Build properties
project_build_sourceEncoding=UTF-8
maven_compiler_source=11
maven_compiler_target=11

# Private repository credentials (configure your own credentials)
# trionesdevUsername=your_username
# trionesdevPassword=your_password
